# Minimal dependencies for the ML pipeline ONLY (app/ml/*.py + the
# app/services modules it calls into: feature_engineering, anomaly_model,
# msr_agent, decision_engine, sca_agent, llm_client).
#
# This is a strict SUBSET of the full application's requirements.txt — it
# deliberately excludes FastAPI, spaCy, sentence-transformers, faster-whisper,
# motor/pymongo, and celery/redis, none of which the training/evaluation
# scripts import. Installing this file alone is enough to run every script
# under app/ml/ — including in a bare Google Colab runtime.
#
# Install with:
#   pip install -r requirements-ml.txt

numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
matplotlib>=3.7.0

pydantic>=2.4.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

httpx>=0.24.0
openai>=1.3.0
google-genai>=1.0.0
